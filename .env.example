PORT=4000
JWT_SECRET=change-me-in-real-deployment
DATABASE_FILE=./data/dev.sqlite3
UPLOADS_DIR=./uploads

# Postgres (RDS) — only used when DB_CLIENT=pg; omit/leave as sqlite for local dev.
# DB_CLIENT=pg
# DB_HOST=table-order-db.cyruyyemc9h0.us-east-1.rds.amazonaws.com
# DB_PORT=5432
# DB_NAME=tableorder
# DB_USER=tableorder_admin
# DB_PASSWORD=   # fetch at runtime from SSM: aws ssm get-parameter --name /table-order/rds/password --with-decryption
